{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "about-window",
  "description": "The About window reads a version string, closes itself, and asks for an update check through the app's own check_for_updates_now command, which runs the updater and its dialogs on the Rust side. The window itself therefore still needs none of the default capability's dialog, updater, process or clipboard permissions.",
  "windows": [
    "about"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
